#include <iostream>
using namespace std;
int main()
{
    int number;
    cin>>number;
    if(number%10==3 || number%10==8)
    {
        cout<<"Lucky Winner";
    }
    else
    {
        cout<<"Not a Lucky Winner";
    }
    return 0;
}